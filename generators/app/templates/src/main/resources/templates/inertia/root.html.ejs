<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= appName %></title>

    <!-- Polyfill pour les navigateurs plus anciens -->
    <script src="https://polyfill.io/v3/polyfill.min.js?features=smoothscroll,NodeList.prototype.forEach,Promise,Object.values,Object.assign"></script>

    <!-- Styles -->
    <% if (frontendFramework === 'React') { %>
    <link rel="stylesheet" href="/assets/css/app.css">
    <% } else if (frontendFramework === 'Vue') { %>
    <link rel="stylesheet" href="/assets/css/app.css">
    <% } else if (frontendFramework === 'Angular') { %>
    <link rel="stylesheet" href="/assets/styles.css">
    <% } %>

    <!-- Scripts -->
    <% if (frontendFramework === 'React') { %>
    <script src="/assets/js/app.js" defer></script>
    <% } else if (frontendFramework === 'Vue') { %>
    <script src="/assets/js/app.js" defer></script>
    <% } else if (frontendFramework === 'Angular') { %>
    <script src="/runtime.js" defer></script>
    <script src="/polyfills.js" defer></script>
    <script src="/main.js" defer></script>
    <% } %>

    <!-- Inertia.js -->
    <script>
        window.initialPage = JSON.parse('<%= new String(java.util.Base64.getEncoder().encode(objectMapper.writeValueAsString(page).getBytes("UTF-8"))) %>');
    </script>
</head>
<body>
    <div id="app" data-page='<%= java.net.URLEncoder.encode(objectMapper.writeValueAsString(page), "UTF-8") %>'></div>
</body>
</html>

