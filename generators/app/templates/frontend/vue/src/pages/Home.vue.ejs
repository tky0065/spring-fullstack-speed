<template>
  <div class="home">
    <h1>Bienvenue dans votre application <%= appName %></h1>
    <p>Cette application a été générée avec Spring-Fullstack-Speed.</p>

    <h2>Technologies utilisées</h2>
    <ul>
      <li>Backend: Spring Boot <%= springBootVersion %></li>
      <li>Frontend: Vue.js 3</li>
      <% if (database && database !== 'H2') { %>
      <li>Base de données: <%= database %></li>
      <% } else { %>
      <li>Base de données: H2 (en mémoire)</li>
      <% } %>
      <% if (includeAuth) { %>
      <li>Authentification: <%= authType %></li>
      <% } %>
    </ul>

    <h2>Les API disponibles</h2>
    <ul>
      <li><a href="/api/hello" target="_blank">/api/hello</a> - API de test</li>
      <% if (includeAuth) { %>
      <li><a href="/api/auth/signin" target="_blank">/api/auth/signin</a> - Connexion</li>
      <li><a href="/api/auth/signup" target="_blank">/api/auth/signup</a> - Inscription</li>
      <% } %>
      <% if (additionalFeatures && additionalFeatures.includes('openapi')) { %>
      <li><a href="/swagger-ui.html" target="_blank">/swagger-ui.html</a> - Documentation API</li>
      <% } %>
    </ul>
  </div>
</template>

<style>
.home {
  max-width: 800px;
  margin: 0 auto;
  padding: 20px;
}

ul {
  margin-left: 20px;
}
</style>
