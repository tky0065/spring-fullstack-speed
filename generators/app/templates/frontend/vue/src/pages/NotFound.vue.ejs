<template>
  <MainLayout title="Page non trouvée">
    <div class="min-h-[70vh] flex flex-col items-center justify-center px-4 py-12">
      <h1 class="text-5xl md:text-8xl font-extrabold text-gray-800 mb-4">404</h1>
      <h2 class="text-2xl md:text-3xl font-semibold text-gray-700 mb-8">Page non trouvée</h2>
      <p class="text-lg text-gray-600 max-w-lg text-center mb-8">
        La page que vous recherchez n'existe pas ou a été déplacée.
      </p>

      <div class="flex gap-4">
        <Button primary href="/">
          Retourner à l'accueil
        </Button>
        <Button @click="goBack">
          Page précédente
        </Button>
      </div>
    </div>
  </MainLayout>
</template>

<script setup lang="ts">
import { useRouter } from 'vue-router';
import MainLayout from '../layouts/MainLayout.vue';
import Button from '../components/ui/Button.vue';

const router = useRouter();

/**
 * Fonction pour retourner à la page précédente
 */
const goBack = () => {
  router.back();
};
</script>
