import React from 'react';
import { Head } from '@inertiajs/react';

/**
 * Types des props pour le composant Home
 */
interface HomeProps {
  appName: string;
}

/**
 * Composant de page d'accueil pour <%= appName %>
 */
export default function Home({ appName }: HomeProps): React.ReactElement {
  return (
    <>
      <Head title="Accueil" />
      <div className="container mx-auto px-4 py-8">
        <div className="bg-white rounded-lg shadow-lg p-6">
          <h1 className="text-3xl font-bold text-gray-800 mb-4">
            Bienvenue sur {appName} !
          </h1>
          <p className="text-gray-600 mb-4">
            Cette application a été générée avec Spring-Fullstack-Speed.
          </p>
          <div className="mt-6">
            <h2 className="text-xl font-semibold text-gray-700 mb-2">
              Prochaines étapes
            </h2>
            <ul className="list-disc pl-5 space-y-2 text-gray-600">
              <li>Modifier cette page dans <code>frontend/src/pages/Home.tsx</code></li>
              <li>Créer de nouvelles pages dans le dossier <code>pages</code></li>
              <li>Personnaliser les styles dans <code>styles</code></li>
              <li>Configurer l'authentification</li>
            </ul>
          </div>
        </div>
      </div>
    </>
  );
}
