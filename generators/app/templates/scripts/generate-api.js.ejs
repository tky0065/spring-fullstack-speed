#!/usr/bin/env node
/**
 * Script pour générer automatiquement les clients API à partir de la spécification OpenAPI
 * Ce script peut être appelé directement via la commande CLI personnalisée
 */

const { spawn } = require('child_process');
const path = require('path');
const fs = require('fs');
const chalk = require('chalk'); // Utiliser chalk pour les messages colorés

// Configuration
const DEFAULT_API_URL = 'http://localhost:8080/v3/api-docs';
const DEFAULT_OUTPUT_DIR = 'src/api';
const DEFAULT_GENERATOR = 'typescript-axios';

/**
 * Affiche un message d'aide pour l'utilisation du générateur
 */
function showHelp() {
  console.log(chalk.blue.bold('📚 OpenAPI Client Generator - Aide'));
  console.log('');
  console.log('Cette commande génère automatiquement des clients API TypeScript à partir de votre spécification OpenAPI.');
  console.log('');
  console.log(chalk.yellow('Usage:'));
  console.log('  sfs generate-api [options]');
  console.log('');
  console.log(chalk.yellow('Options:'));
  console.log(`  --url    URL de la spécification OpenAPI (défaut: ${DEFAULT_API_URL})`);
  console.log(`  --output Répertoire de sortie (défaut: ${DEFAULT_OUTPUT_DIR})`);
  console.log(`  --gen    Générateur à utiliser (défaut: ${DEFAULT_GENERATOR})`);
  console.log('  --help   Affiche ce message d\'aide');
  console.log('');
  console.log(chalk.yellow('Générateurs disponibles:'));
  console.log('  typescript-fetch    - Client TypeScript utilisant Fetch API');
  console.log('  typescript-axios    - Client TypeScript utilisant Axios');
  console.log('  typescript-angular  - Client TypeScript pour Angular');
  console.log('  typescript-react-query - Client TypeScript avec React Query');
  console.log('');
  console.log(chalk.yellow('Exemples:'));
  console.log('  sfs generate-api');
  console.log('  sfs generate-api --url=http://myapi:8080/v3/api-docs');
  console.log('  sfs generate-api --output=src/api-client');
  console.log('  sfs generate-api --gen=typescript-fetch');
  console.log('');
}

/**
 * Parse les arguments de la ligne de commande
 */
function parseArguments() {
  const args = process.argv.slice(2);
  const options = {
    url: DEFAULT_API_URL,
    output: DEFAULT_OUTPUT_DIR,
    generator: DEFAULT_GENERATOR
  };

  args.forEach(arg => {
    if (arg === '--help') {
      showHelp();
      process.exit(0);
    } else if (arg.startsWith('--url=')) {
      options.url = arg.split('=')[1];
    } else if (arg.startsWith('--output=')) {
      options.output = arg.split('=')[1];
    } else if (arg.startsWith('--gen=')) {
      options.generator = arg.split('=')[1];
    }
  });

  return options;
}

/**
 * Génère les clients API à partir de la spécification OpenAPI
 */
async function generateApi() {
  try {
    const options = parseArguments();

    console.log(chalk.blue.bold('🚀 Génération des clients API depuis OpenAPI...'));
    console.log(chalk.yellow(`URL de l'API:  ${options.url}`));
    console.log(chalk.yellow(`Sortie:        ${options.output}`));
    console.log(chalk.yellow(`Générateur:    ${options.generator}`));
    console.log('');

    // S'assurer que le répertoire de sortie existe
    const outputDir = path.resolve(process.cwd(), options.output);
    if (!fs.existsSync(path.dirname(outputDir))) {
      fs.mkdirSync(path.dirname(outputDir), { recursive: true });
    }

    // Exécuter la commande openapi-generator-cli
    const generator = spawn('npx', [
      '@openapitools/openapi-generator-cli',
      'generate',
      '-i', options.url,
      '-g', options.generator,
      '-o', options.output
    ], { stdio: 'inherit' });

    // Gérer la fin de la génération
    generator.on('close', (code) => {
      if (code === 0) {
        console.log('');
        console.log(chalk.green.bold('✅ Génération des clients API terminée avec succès!'));
        console.log(`Les clients API ont été générés dans le répertoire: ${chalk.yellow(options.output)}`);
        console.log('');
        console.log(chalk.blue('📝 Utilisation des API générées:'));
        console.log('');

        if (options.generator.includes('typescript-axios')) {
          console.log(chalk.gray(`import { DefaultApi, Configuration } from './${options.output}';`));
          console.log(chalk.gray(`const config = new Configuration({ basePath: 'http://localhost:8080' });`));
          console.log(chalk.gray(`const api = new DefaultApi(config);`));
          console.log(chalk.gray(`// Exemple d'utilisation:`));
          console.log(chalk.gray(`api.someEndpoint().then(response => console.log(response.data));`));
        } else if (options.generator.includes('typescript-fetch')) {
          console.log(chalk.gray(`import { DefaultApi, Configuration } from './${options.output}';`));
          console.log(chalk.gray(`const config = new Configuration({ basePath: 'http://localhost:8080' });`));
          console.log(chalk.gray(`const api = new DefaultApi(config);`));
          console.log(chalk.gray(`// Exemple d'utilisation:`));
          console.log(chalk.gray(`api.someEndpoint().then(response => console.log(response));`));
        } else if (options.generator.includes('typescript-angular')) {
          console.log(chalk.gray(`// Dans votre module Angular:`));
          console.log(chalk.gray(`import { ApiModule, Configuration } from './${options.output}';`));
          console.log(chalk.gray(`@NgModule({`));
          console.log(chalk.gray(`  imports: [`));
          console.log(chalk.gray(`    ApiModule.forRoot(() => new Configuration({ basePath: 'http://localhost:8080' }))`));
          console.log(chalk.gray(`  ]`));
          console.log(chalk.gray(`})`));
          console.log(chalk.gray(`export class AppModule {}`));
          console.log('');
          console.log(chalk.gray(`// Dans votre service/composant:`));
          console.log(chalk.gray(`import { DefaultService } from './${options.output}';`));
          console.log(chalk.gray(`constructor(private api: DefaultService) {}`));
          console.log(chalk.gray(`// Exemple d'utilisation:`));
          console.log(chalk.gray(`this.api.someEndpoint().subscribe(result => console.log(result));`));
        }

        console.log('');
      } else {
        console.error(chalk.red.bold('❌ Erreur lors de la génération des clients API'));
        console.error(`Code d'erreur: ${code}`);
      }
    });

    generator.on('error', (error) => {
      console.error(chalk.red.bold('❌ Erreur lors de l\'exécution du générateur:'));
      console.error(error.message);
    });

  } catch (error) {
    console.error(chalk.red.bold('❌ Erreur:'), error.message);
    process.exit(1);
  }
}

// Exécuter le script
generateApi();
